import { analyticsModuleAzurePlugin } from './plugin';

describe('analytics-module-azure', () => {
  it('should export plugin', () => {
    expect(analyticsModuleAzurePlugin).toBeDefined();
  });
});
